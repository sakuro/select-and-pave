-- Placeholder so `mise run test` and CI pass on a freshly generated MOD.
-- Replace with real specs under spec/.
describe("scaffold", function()
  it("has a working test lane", function()
    assert.are.equal(2, 1 + 1)
  end)
end)
